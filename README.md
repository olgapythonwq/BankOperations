# BankOperations

Краткое описание проекта, его цели и задачи.\
Разработать приложение для анализа транзакций, которые находятся в Excel-файле. Приложение будет генерировать JSON-данные для веб-страниц, формировать Excel-отчеты, а также предоставлять другие сервисы.

### Задачи по категориям:
##### 1. Веб-страницы:
-	Главная
-	События
##### 2. Сервисы:
-	Выгодные категории повышенного кешбэка
-	Инвесткопилка
-	Простой поиск
-	Поиск по телефонным номерам
-	Поиск переводов физическим лицам
##### 3. Отчеты:
-	Траты по категории
-	Траты по дням недели
-	Траты в рабочий/выходной день

Статус: в разработке

## Содержание
[Установка](#Установка)  
[Использование](#Использование)  
[Вклад](#Вклад)

## <a id="title1">Установка</a>

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/olgapythonwq/BankOperations.git
   ```
2. Перейдите в директорию проекта:
   ```bash
   cd ваш_проект
   ```
3. Установите необходимые зависимости:
   ```bash
   pip install -r requirements.txt
   ```
   
## <a id="title1">Использование</a>

### Функции:
- Функция, приветствующая пользователя по разному в зависимости от времени суток
- Функция, принимающая строку с датой и временем в формате YYYY-MM-DD HH:MM:SS и возвращающая ответ в виде словаря
- Функции, принимающая путь до EXCEL файла и возвращающая объект DataFrame с данными об операциях
- Функция, фильтрующая транзакции по дате. Формат дат: YYYY-MM-DD HH:MM:SS
- Функция, принимающая объект DataFrame с данными об операциях и возвращающая данные по картам
- Функция, принимающая объект DataFrame с данными об операциях и возвращающая топ-5 транзакций
- Функция, принимающая путь до json файла и возвращающая список валют
- Функция, принимающая список валют и возвращающая курсы валют от API
- Функция, принимающая валюту, получающая её курс от API
- Функция, принимающая путь до json файла и возвращающая список тикеров
- Функция, принимающая список тикеров и возвращающая цены тикеров от API
- Функция, принимающая тикер, получающая его цену от API
- Функция, анализирующая какие категории были наиболее выгодными для получения кешбэка
- Функция, возвращающая траты по заданной категории за последние три месяца
- Декоратор, записывающий полученный DataFrame в Excel-файл

#### Примеры использования функций:

```python
from datetime import datetime


def greeting() -> str:
    """Функция, приветствующая пользователя по разному в зависимости от времени суток"""
    now = datetime.now()  # Получаем текущие дату и время
    if 6 <= now.hour < 12:
        greeting_phrase = "Доброе утро"
    elif 12 <= now.hour < 18:
        greeting_phrase = "Добрый день"
    elif 18 <= now.hour < 24:
        greeting_phrase = "Добрый вечер"
    else:
        greeting_phrase = "Доброй ночи"
    return greeting_phrase

```
## <a id="title1">Тестирование</a>

Все тесты находятся в пакете tests. Фикстуры вынесены в файл conftest.py. Процент покрытия тестами находится в файле 
index.html (можно открыть в браузере). Тестирование проводилось с использованием библиотеки pytest.

Для запуска тестов выполните команду:
```bash
pytest
```


## <a id="title1">Вклад</a>

Если вы хотите внести свой вклад, пожалуйста, создайте форк репозитория и отправьте пул-реквест.
